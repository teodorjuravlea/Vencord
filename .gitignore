dist
node_modules

*.exe
vencord_installer

.idea
.DS_Store

yarn.lock
bun.lock
package-lock.json

*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
.pnpm-debug.log*
*.tsbuildinfo

src/userplugins

ExtensionCache/
/settings

src/plugins/betterCamera.desktop

# DashBeam wasm binary — build artifact distributed via the
# VencordDashBeamWasm GitHub release (auto-downloaded with a versioned cache);
# the patched JS glue next to it IS tracked
src/plugins/dashbeam.desktop/wasm/wasm_bridge_bg.wasm
